name: Hello world

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Build step
        run: echo "Welcome sounabh"

  deploy:
    runs-on: ubuntu-latest  
    needs : build 

    steps: 
      - name: deploy step
        run: echo "Welcome deploy" 